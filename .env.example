# LLM provider (OpenAI-compatible API)
LLM_API_KEY=your-llm-api-key-here
LLM_BASE_URL=https://aisupervisor.ru/v1
LLM_MODEL=qwen3_4b_instruct

# Telegram
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
TELEGRAM_ALLOWED_USER_IDS=[]

# PostgreSQL (overridden by docker-compose for container networking)
POSTGRES_DSN=postgresql://agent:agent@postgres:5432/agent_db

# Docker sandbox
DOCKER_SANDBOX_IMAGE=agent-sandbox:latest
DOCKER_EXECUTION_TIMEOUT=60

# Web UI
WEB_HOST=0.0.0.0
WEB_PORT=8080
WEB_ENABLED=true

# Logging
LOG_LEVEL=INFO
# Prompt block debug logging (comma-separated: system,tool_descriptions,planning_addon,settings,datetime,insights,task_context,conversation,user_request,full_prompt,response or "all")
# LOG_PROMPT_BLOCKS=
